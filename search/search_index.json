{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Warp Engine","text":"<p>Auto-discovery REST CRUD API generator with LLM-powered database catalog intelligence.</p> <p>Warp connects to your database, discovers tables and columns, and generates a fully functional REST API with filtering, sorting, pagination, and authentication. Its catalog intelligence layer uses LLMs to generate human-readable descriptions, semantic types, and multi-language documentation for your schema \u2014 and injects all of it into the OpenAPI spec so downstream LLMs and tools get rich context automatically.</p>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li>Auto-discovery of tables \u2192 full CRUD endpoints (PostgreSQL, MySQL &amp; SQL Server;   other ODBC sources best effort)</li> <li>Filtering / sorting / pagination with a safe, parameterized query layer</li> <li>API-key auth with role-based permissions</li> <li>Catalog intelligence \u2014 LLM-generated descriptions, semantic types, tags,   relationships, multi-language docs, and a human-in-the-loop review workflow</li> <li>OpenAPI enrichment with <code>x-llm-context</code> extensions</li> <li>Safe by default \u2014 see Production Deployment</li> </ul>"},{"location":"#get-started","title":"Get started","text":"<p>See the README for installation and configuration. For a hardened production setup, read Production Deployment. For the rationale behind key design choices, see the Architecture Decisions.</p>"},{"location":"architecture/","title":"Architecture","text":"<p>Warp is a hexagonal (ports &amp; adapters) application. Dependencies point inward and are enforced by import-linter contracts in <code>pyproject.toml</code>; run them locally with <code>lint-imports</code>.</p>"},{"location":"architecture/#layers-outside-inside","title":"Layers (outside \u2192 inside)","text":"Layer Package(s) Responsibility Entry points <code>warp.main</code>, <code>warp.cli</code> The ASGI target / uvicorn runner and the <code>warp-catalog</code> console script; the only modules that call the composition root Composition root <code>warp.infrastructure</code> <code>bootstrap.build_container()</code> wires outbound adapters into a <code>Container</code>; <code>config_loader</code> reads YAML + <code>${ENV}</code>; <code>logging</code> setup Inbound adapters <code>warp.adapters.inbound.http</code>, <code>warp.adapters.inbound.cli</code> FastAPI app/lifespan, routes, auth dependencies, error mapping; Click commands Outbound adapters <code>warp.adapters.outbound.db</code>, <code>.llm</code>, <code>.catalog_store</code>, <code>.export</code> PostgreSQL/MySQL/ODBC (SQL Server) gateways, the <code>Dialect</code> table + SQL helpers, metadata readers, LLM providers, file-based catalog repository, exporters Application <code>warp.application</code> Use-case services, ports (Protocols), configuration models, <code>Container</code> Domain <code>warp.domain</code> Schema/catalog entities, review transitions, naming rules, sample/PII rules, filtering/sorting/pagination value objects, errors <p>Contracts: a <code>layers</code> contract over the five layers, <code>independence</code> between inbound and outbound adapters (and between http and cli), and <code>forbidden</code> contracts keeping frameworks and drivers out of <code>domain</code> and <code>application</code>.</p>"},{"location":"architecture/#ports-and-their-adapters","title":"Ports and their adapters","text":"Port (<code>warp.application.ports</code>) Used by Implemented by <code>DatabaseGateway</code>, <code>SqlReader</code>, <code>DatabaseGatewayFactory</code> CRUD, schema discovery, analysis, raw query, HTTP lifespan <code>adapters.outbound.db.{postgres,mysql,odbc,factory}</code> <code>CommentSource</code>, <code>SampleSource</code> <code>CatalogAnalysisService</code> <code>adapters.outbound.db.{comment_reader,sample_reader}</code> <code>CatalogRepository</code> review / cross-reference / analysis services, routes, CLI <code>adapters.outbound.catalog_store.file_store</code> <code>TextGenerator</code> <code>CatalogAnalysisService</code> <code>adapters.outbound.llm.client.LLMClient</code> (OpenAI, Anthropic, Gemini, Ollama providers) <code>CatalogExporter</code> <code>CatalogExportService</code> <code>adapters.outbound.export.{json,yaml,markdown}</code>"},{"location":"architecture/#composition-root","title":"Composition root","text":"<pre><code>warp.main / warp.cli\n  \u2192 infrastructure.config_loader.load_config()      Settings\n  \u2192 infrastructure.bootstrap.build_container()      Container\n      repository, review, export, gateway_factory,\n      text_generator_factory, analysis_factory\n  \u2192 adapters.inbound.http.create_app(container_factory, env)\n    adapters.inbound.cli.main (factory injected via context_settings)\n</code></pre> <p><code>Container.open_gateway()</code> / <code>open_analysis()</code> / <code>pipeline()</code> own the lifetimes of what they open; tests pass fakes to <code>build_container()</code> instead of patching modules.</p>"},{"location":"architecture/#request-flow-crud","title":"Request flow (CRUD)","text":"<pre><code>HTTP request\n  \u2192 adapters.inbound.http.routes.crud   (auth dependency; parse id/fields/filters by column kind)\n    \u2192 application.services.crud        (mass-assignment whitelist)\n      \u2192 DatabaseGateway port\n        \u2192 adapters.outbound.db.postgres | mysql | odbc   (SafeQueryBuilder over a Dialect, identifier whitelist, bound params)\n</code></pre>"},{"location":"architecture/#catalog-intelligence-flow","title":"Catalog intelligence flow","text":"<pre><code>adapters.inbound.http.routes.catalog / adapters.inbound.cli\n  \u2192 Container.open_analysis(db)\n    \u2192 application.services.catalog_analysis.CatalogAnalysisService\n        CommentSource + SampleSource (PII policy: domain.samples)\n        TextGenerator (application.prompts)\n        CrossReferenceService (other catalogs)\n        CatalogReviewService \u2192 domain.catalog_review \u2192 CatalogRepository\n  \u2192 application.services.openapi_enrichment (x-llm-context into the OpenAPI spec, approved catalogs only)\n</code></pre> <p>Engine differences (placeholders, quoting, <code>RETURNING</code>/<code>OUTPUT</code>, pagination, catalog queries) live in one frozen <code>Dialect</code> per engine (<code>adapters.outbound.db.dialect</code>), consumed by the builder, the parameter binder and the metadata readers \u2014 see ADR-0008.</p> <p>See the Architecture Decisions, in particular ADR-0007, for the rationale.</p>"},{"location":"deployment/","title":"Production Deployment (secure defaults)","text":"<p>Warp fails safe: when <code>APP_ENV=production</code> the app refuses to start with unsafe configuration.</p>"},{"location":"deployment/#checklist","title":"Checklist","text":"<ol> <li>Secrets via environment, never in the repo. Copy <code>.env.example</code> \u2192 <code>.env</code>    (gitignored) and set strong values. Never commit real credentials or keys.</li> <li><code>APP_ENV=production</code> \u2014 disables <code>/docs</code> &amp; <code>/redoc</code>, switches logs to JSON,    and enables the startup safety checks below.</li> <li>Authentication on \u2014 <code>settings.auth.enabled: true</code> with real API keys via    <code>API_KEY_*</code> env vars. Startup is refused if auth is off in production.</li> <li>Explicit CORS allowlist \u2014 <code>CORS_ORIGINS=https://app.example.com,...</code>.    <code>*</code> is rejected in production; credentials are only sent with an explicit    allowlist.</li> <li>Raw SQL endpoint off \u2014 <code>enable_raw_query: false</code> (default). Startup is    refused if it is enabled in production. If you must enable it, configure a    read-only database role via <code>databases[].readonly_username</code> /    <code>readonly_password</code> (env <code>DB_READONLY_USER</code> / <code>DB_READONLY_PASS</code>); raw queries    then run as that role, so a whitelist bypass still cannot write.</li> <li>Run the production image \u2014 the <code>Dockerfile</code> is multi-stage, runs as a    non-root user, ships no dev dependencies, and has no <code>--reload</code>. Mount a    hardened <code>config/database.yaml</code> and pass secrets via the environment.</li> <li>Reproducible installs \u2014 <code>uv pip sync --require-hashes requirements.lock</code> for deterministic,    hash-verified dependencies.</li> <li>Least-privilege DB account \u2014 grant only what the API needs; restrict    write access to the tables that should be writable.</li> <li>Catalog endpoints are authenticated \u2014 <code>/api/v1/catalog/*</code> needs a key    with <code>read</code> (views), <code>create</code> (analysis), <code>update</code> (edits/approvals) or    <code>delete</code>. Keep <code>catalog.openapi_include_examples: false</code> unless the spec    audience may see real row values.</li> </ol>"},{"location":"deployment/#startup-safety-checks","title":"Startup safety checks","text":"<p><code>validate_production_config()</code> (in <code>warp.application.config</code>) runs during app startup. With <code>APP_ENV=production</code> it raises <code>ConfigurationError</code> (refusing to start) when any of these hold:</p> <ul> <li><code>auth.enabled</code> is <code>false</code></li> <li><code>settings.enable_raw_query</code> is <code>true</code></li> <li><code>CORS_ORIGINS</code> contains <code>*</code></li> <li><code>/openapi.json</code> is in <code>auth.public_paths</code> (the enriched spec exposes catalog   descriptions and <code>x-llm-context</code>) unless <code>auth.allow_public_openapi: true</code></li> </ul> <p>This guarantees a misconfigured production deployment fails loudly at boot rather than silently exposing the API.</p>"},{"location":"deployment/#always-on-protections","title":"Always-on protections","text":"<p>Parameterized queries, mass-assignment protection, timing-safe API-key checks, and PII masking before any cloud LLM call are enabled by default regardless of environment. See the Security Policy.</p>"},{"location":"reference/","title":"API Reference","text":"<p>Selected internal modules, generated from docstrings.</p>"},{"location":"reference/#sql-identifiers","title":"SQL identifiers","text":"<p>The single source of truth for validating/quoting dynamic SQL identifiers.</p> <p>Centralized SQL identifier validation and quoting.</p> <p>Every dynamic identifier (table or column name) that gets interpolated into a SQL string MUST pass through :func:<code>sanitize_identifier</code> / :func:<code>quote_identifier</code>. Values are always sent as bound parameters and must never go through these helpers. Keeping this logic in one place avoids divergent, weaker copies (e.g. <code>str.replace</code>-based \"sanitizers\") across adapters.</p>"},{"location":"reference/#warp.adapters.outbound.db.identifiers.quote_identifier","title":"<code>quote_identifier(name, dialect='postgresql')</code>","text":"<p>Validate and quote an identifier for the given SQL dialect.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The identifier to validate and quote.</p> required <code>dialect</code> <code>str | Dialect</code> <p>Target dialect name or :class:<code>Dialect</code> (<code>mysql</code>/<code>mariadb</code> use backticks, <code>mssql</code>/<code>sqlserver</code> square brackets, everything else ANSI double quotes).</p> <code>'postgresql'</code> <p>Returns:</p> Type Description <code>str</code> <p>The safely quoted identifier.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the identifier is not valid (see :func:<code>sanitize_identifier</code>).</p>"},{"location":"reference/#warp.adapters.outbound.db.identifiers.sanitize_identifier","title":"<code>sanitize_identifier(name)</code>","text":"<p>Validate a SQL identifier (table/column name).</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The identifier to validate.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The identifier unchanged when it is safe.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the identifier contains anything other than ASCII letters, digits, and underscores (and does not start with a digit). This is what prevents identifier/SQL injection through dynamic table and column names.</p>"},{"location":"reference/#named-parameters-for-raw-sql","title":"Named parameters for raw SQL","text":"<p>Rewrite <code>:name</code> placeholders into the dialect's positional form.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>SQL text with <code>:name</code> placeholders.</p> required <code>params</code> <code>dict[str, Any] | None</code> <p>Values by name. <code>None</code>/empty leaves the query untouched.</p> required <code>dialect</code> <code>str | Dialect</code> <p>Dialect name or :class:<code>Dialect</code>: <code>postgresql</code> (<code>$N</code>), <code>mysql</code> (<code>%s</code>), <code>mssql</code>/<code>odbc</code> (<code>?</code>).</p> required <p>Returns:</p> Type Description <code>tuple[str, list[Any]]</code> <p>The rewritten SQL and the positional values.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>On an unknown dialect, a placeholder without a value, or a value without a placeholder.</p>"},{"location":"reference/#ports","title":"Ports","text":"<p>Database ports: what the application needs from a SQL database.</p> <p>Catalog persistence port.</p> <p>Text-generation port (an LLM behind a provider-agnostic interface).</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway","title":"<code>DatabaseGateway</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Full database port: connection lifecycle, introspection and CRUD.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.is_connected","title":"<code>is_connected</code>  <code>property</code>","text":"<p>Whether the underlying pool is open.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.connect","title":"<code>connect()</code>  <code>async</code>","text":"<p>Open the connection pool.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.delete","title":"<code>delete(table, id_column, id_value)</code>  <code>async</code>","text":"<p>Delete one record; True when a row was removed.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.disconnect","title":"<code>disconnect()</code>  <code>async</code>","text":"<p>Close the connection pool.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.execute_query","title":"<code>execute_query(query, params=None)</code>  <code>async</code>","text":"<p>Run a SQL statement (<code>:name</code> placeholders) and return rows as dicts.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.get_table_schema","title":"<code>get_table_schema(table)</code>  <code>async</code>","text":"<p>Describe a table (columns, primary key, foreign keys, indexes).</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.get_tables","title":"<code>get_tables()</code>  <code>async</code>","text":"<p>List base tables.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.insert","title":"<code>insert(table, data)</code>  <code>async</code>","text":"<p>Insert a record and return it.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.select","title":"<code>select(table, columns=None, filters=None, pagination=None, sort=None)</code>  <code>async</code>","text":"<p>Select records and the total count.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.select_by_id","title":"<code>select_by_id(table, id_column, id_value, columns=None)</code>  <code>async</code>","text":"<p>Select one record by primary key.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGateway.update","title":"<code>update(table, id_column, id_value, data)</code>  <code>async</code>","text":"<p>Update one record and return it, or None when it does not exist.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGatewayFactory","title":"<code>DatabaseGatewayFactory</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Builds a gateway for a configured database (the adapter picks the driver).</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGatewayFactory.create","title":"<code>create(config)</code>","text":"<p>Instantiate (but do not connect) a gateway for <code>config</code>.</p>"},{"location":"reference/#warp.application.ports.database.DatabaseGatewayFactory.get_supported_types","title":"<code>get_supported_types()</code>","text":"<p>Database type names this factory can build.</p>"},{"location":"reference/#warp.application.ports.database.SqlReader","title":"<code>SqlReader</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>The narrow read port used by metadata readers and the raw-query use case.</p>"},{"location":"reference/#warp.application.ports.database.SqlReader.execute_query","title":"<code>execute_query(query, params=None)</code>  <code>async</code>","text":"<p>Run a SQL statement (<code>:name</code> placeholders) and return rows as dicts.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository","title":"<code>CatalogRepository</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Stores and retrieves whole catalogs by database name.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.delete","title":"<code>delete(db_name)</code>","text":"<p>Remove a catalog; True when it existed.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.get_index","title":"<code>get_index()</code>","text":"<p>Summary index of stored catalogs.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.list_catalogs","title":"<code>list_catalogs()</code>","text":"<p>Names of every stored catalog.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.load","title":"<code>load(db_name)</code>","text":"<p>Load a catalog, or None when it does not exist.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.load_or_raise","title":"<code>load_or_raise(db_name)</code>","text":"<p>Load a catalog or raise <code>CatalogNotFoundError</code>.</p>"},{"location":"reference/#warp.application.ports.catalog_repository.CatalogRepository.save","title":"<code>save(catalog, format=None)</code>","text":"<p>Persist a catalog; returns where it was written.</p>"},{"location":"reference/#warp.application.ports.text_generation.TextGenerator","title":"<code>TextGenerator</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Generates text/JSON from prompts; closed when the use case is done.</p>"},{"location":"reference/#warp.application.ports.text_generation.TextGenerator.close","title":"<code>close()</code>  <code>async</code>","text":"<p>Release provider resources.</p>"},{"location":"reference/#warp.application.ports.text_generation.TextGenerator.generate","title":"<code>generate(prompt, system_prompt=None, temperature=None, max_tokens=None, response_format=None)</code>  <code>async</code>","text":"<p>Generate a completion.</p>"},{"location":"reference/#warp.application.ports.text_generation.TextGenerator.generate_json","title":"<code>generate_json(prompt, system_prompt=None)</code>  <code>async</code>","text":"<p>Generate a completion expected to be a JSON document.</p>"},{"location":"reference/#composition-root","title":"Composition root","text":"<p>Services and port implementations for one configuration.</p> <p>Wire the default adapters; any of them can be replaced (tests, embedding).</p>"},{"location":"reference/#warp.application.container.Container.database","title":"<code>database(name)</code>","text":"<p>Configuration of the database called <code>name</code> (404-style error if unknown).</p>"},{"location":"reference/#warp.application.container.Container.open_analysis","title":"<code>open_analysis(db_name, *, gateway=None)</code>  <code>async</code>","text":"<p>Yield an analysis service for <code>db_name</code>.</p> <p>A <code>gateway</code> passed in is reused (and not closed); otherwise one is opened for the duration. The text generator is always created and closed here.</p> <p>Raises:</p> Type Description <code>DatabaseNotConfiguredError</code> <p>If <code>db_name</code> is not configured.</p>"},{"location":"reference/#warp.application.container.Container.open_gateway","title":"<code>open_gateway(db_config)</code>  <code>async</code>","text":"<p>Create, connect and finally disconnect a gateway for <code>db_config</code>.</p>"},{"location":"reference/#warp.application.container.Container.pipeline","title":"<code>pipeline()</code>","text":"<p>Pipeline service bound to this container.</p>"},{"location":"reference/#configuration","title":"Configuration","text":"<p>Check security-sensitive configuration for production deployments.</p> <p>Returns a list of human-readable violation messages. The list is only populated when <code>app_env == \"production\"</code>; in any other environment the same options are legitimate defaults and no violations are reported.</p> <p>The caller is expected to refuse startup (fail-safe) when the returned list is non-empty.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Settings</code> <p>Loaded application settings.</p> required <code>app_env</code> <code>str</code> <p>The current <code>APP_ENV</code> value.</p> required <code>cors_origins</code> <code>list[str] | None</code> <p>The effective CORS allowlist (e.g. from <code>CORS_ORIGINS</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of violation messages (empty when the configuration is safe).</p>"},{"location":"adr/","title":"Architecture Decision Records","text":"<p>These ADRs capture significant architectural and process decisions, following the lightweight format popularized by Michael Nygard.</p> # Title Status 0001 Layered architecture enforced by import-linter Superseded by 0007 0002 Fail-safe production configuration validation Accepted 0003 Dependency management: ranges + hashed lockfile Accepted 0004 LLM sample-data privacy (cloud opt-in + PII masking) Accepted 0005 CI quality gates: pragmatic, then strict Accepted 0006 Distribution name vs. the import package Accepted 0007 Hexagonal architecture (ports &amp; adapters) Accepted 0008 Dialect abstraction and the ODBC (SQL Server) adapter Accepted"},{"location":"adr/0001-layered-architecture/","title":"1. Layered architecture enforced by import-linter","text":"<ul> <li>Status: Superseded by ADR-0007</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0001-layered-architecture/#context","title":"Context","text":"<p>The codebase grew organically and had at least one circular dependency (<code>llm</code> \u2194 <code>enrichment</code>). Without an enforced dependency direction, low-level modules risk importing high-level ones, making the system hard to reason about, test, and reuse.</p>"},{"location":"adr/0001-layered-architecture/#decision","title":"Decision","text":"<p>Define explicit layers and enforce their dependency direction with import-linter contracts in <code>pyproject.toml</code> (<code>[tool.importlinter]</code>), run as a blocking CI gate (<code>lint-imports</code>):</p> <ul> <li><code>core</code> and <code>config</code> are the lowest layers and import nothing else in <code>warp</code>.</li> <li><code>database</code> imports no app/domain modules.</li> <li><code>catalog</code> must not depend on <code>llm</code>, <code>enrichment</code>, <code>api</code>, or <code>integration</code>.</li> <li><code>schema</code> and <code>utils</code> must not depend on <code>api</code> or <code>integration</code>.</li> </ul> <p>The <code>llm</code> \u2194 <code>enrichment</code> runtime cycle was broken by importing <code>TableSamples</code> under <code>TYPE_CHECKING</code> in <code>llm/prompts.py</code>.</p>"},{"location":"adr/0001-layered-architecture/#consequences","title":"Consequences","text":"<ul> <li>Dependency direction is verifiable and regressions are caught in CI.</li> <li>New code must respect the contracts (see CONTRIBUTING).</li> <li>A few contracts are intentionally conservative; they can be tightened (e.g. a   strict total ordering) as the architecture stabilizes.</li> </ul>"},{"location":"adr/0002-fail-safe-production-config/","title":"2. Fail-safe production configuration validation","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0002-fail-safe-production-config/#context","title":"Context","text":"<p>The defaults convenient for development (auth disabled, raw SQL endpoint on, <code>CORS_ORIGINS=*</code>, docs exposed) are dangerous in production. Relying on operators to remember to flip every switch is error-prone and fails silently.</p>"},{"location":"adr/0002-fail-safe-production-config/#decision","title":"Decision","text":"<p>Add <code>validate_production_config()</code> (in <code>warp.config.settings</code>), called during app startup. When <code>APP_ENV=production</code>, it raises <code>ConfigurationError</code> and refuses to start if any unsafe condition holds:</p> <ul> <li><code>auth.enabled</code> is <code>false</code></li> <li><code>settings.enable_raw_query</code> is <code>true</code></li> <li><code>CORS_ORIGINS</code> contains <code>*</code></li> </ul> <p>These options keep their developer-friendly defaults outside production. The shipped image defaults to <code>APP_ENV=production</code>, so a misconfigured deployment fails loudly at boot.</p>"},{"location":"adr/0002-fail-safe-production-config/#consequences","title":"Consequences","text":"<ul> <li>Insecure production deployments fail fast and visibly instead of silently   exposing the API.</li> <li>Operators must provide explicit, safe values (documented in   <code>docs/deployment.md</code> and <code>.env.example</code>).</li> <li>The check is centralized and unit-tested.</li> </ul>"},{"location":"adr/0003-dependency-management/","title":"3. Dependency management: ranges + hashed lockfile","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0003-dependency-management/#context","title":"Context","text":"<p>The project is consumed both as a library (<code>pip install</code>) and deployed as an application (Docker). Libraries want flexible version ranges to avoid conflicts for downstream consumers; deployments want exact, reproducible, verifiable dependencies.</p>"},{"location":"adr/0003-dependency-management/#decision","title":"Decision","text":"<ul> <li>Keep flexible lower-bound ranges (<code>&gt;=</code>) in <code>pyproject.toml</code> for library   consumers.</li> <li>Generate two universal, hashed lock files with <code>make lock</code>:   <code>requirements.lock</code> (all extras; CI and local development) and   <code>requirements-prod.lock</code> (runtime + <code>llm</code> + <code>odbc</code>; the Docker image).</li> <li>Install from the locks with <code>--require-hashes</code> (<code>uv pip sync</code>) in CI and in   the Dockerfile builder stage; never resolve from ranges in a deployment.</li> <li>Migrated the Gemini provider off the EOL <code>google-generativeai</code> SDK to   <code>google-genai</code>.</li> <li>Dependabot keeps both Python deps and the SHA-pinned GitHub Actions current.</li> </ul>"},{"location":"adr/0003-dependency-management/#consequences","title":"Consequences","text":"<ul> <li>Library consumers retain resolution flexibility; deployments are deterministic   and tamper-evident (hashes).</li> <li>Both lock files must be regenerated (<code>make lock</code>) whenever <code>pyproject.toml</code>   dependencies change; CI installs from them, so a stale lock fails fast.</li> </ul>"},{"location":"adr/0004-llm-sample-data-privacy/","title":"4. LLM sample-data privacy (cloud opt-in + PII masking)","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0004-llm-sample-data-privacy/#context","title":"Context","text":"<p>Catalog intelligence sends table samples to an LLM to generate descriptions. Sample rows can contain personal or sensitive data. Sending that to a third-party (cloud) LLM provider is a privacy risk that should never happen implicitly.</p>"},{"location":"adr/0004-llm-sample-data-privacy/#decision","title":"Decision","text":"<p>In <code>warp.enrichment.analyzer</code>, gate sample data before it reaches the prompt:</p> <ul> <li>Cloud opt-in: cloud providers (<code>openai</code>, <code>anthropic</code>, <code>gemini</code>) receive no   raw samples unless <code>analysis.share_samples_with_cloud_llm</code> is enabled. Local   providers (Ollama) are exempt.</li> <li>PII masking: when <code>analysis.mask_pii_samples</code> is enabled (default),   values of columns whose names match PII patterns (email, phone, ssn, password,   token, \u2026) are masked to <code>***</code> before any LLM call.</li> </ul> <p>The unmodified samples are still used to populate the catalog locally.</p>"},{"location":"adr/0004-llm-sample-data-privacy/#consequences","title":"Consequences","text":"<ul> <li>No sensitive sample data leaves the machine to a cloud LLM by default.</li> <li>Pattern-based PII detection is heuristic (name-based); operators can extend   <code>analysis.pii_column_patterns</code>. Semantic-type-based masking is a possible   future enhancement once a catalog already exists.</li> </ul>"},{"location":"adr/0004-llm-sample-data-privacy/#addendum-090-storage-time-masking-and-openapi-examples","title":"Addendum (0.9.0): storage-time masking and OpenAPI examples","text":"<p>Masking the LLM prompt was not enough: the stored catalog was re-published through the draft API and injected into <code>/openapi.json</code> as <code>example</code> values. Sample values of PII columns \u2014 matched by <code>analysis.pii_column_patterns</code> on the name or by the LLM's <code>semantic_type</code> (<code>email</code>, <code>phone</code>, <code>name</code>, <code>address</code>) \u2014 are therefore dropped before a <code>ColumnCatalogEntry</code> is built (<code>warp.domain.samples.samples_for_storage</code>). Writing examples into the OpenAPI spec is a separate opt-in, <code>catalog.openapi_include_examples</code> (default false), and the spec route itself is protected by the auth manager (see the security policy).</p>"},{"location":"adr/0005-ci-quality-gates/","title":"5. CI quality gates: pragmatic, then strict","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0005-ci-quality-gates/#context","title":"Context","text":"<p>When CI was introduced, the codebase did not yet pass its own intended gates: ~597 ruff errors, 147 <code>mypy --strict</code> errors, ~49% coverage, and 4 broken tests. Making every gate blocking immediately would have left CI red and blocked all work; making none blocking would let quality keep drifting.</p>"},{"location":"adr/0005-ci-quality-gates/#decision","title":"Decision","text":"<p>Introduce CI green from day one, then ratchet each gate to blocking as the debt was cleared:</p> <ol> <li>Start: <code>pytest</code> blocking; ruff / mypy / coverage non-blocking (reported).</li> <li>Clear the backlog incrementally \u2014 fix the broken tests, clean ruff to zero,    fix all <code>mypy --strict</code> errors, raise coverage to \u226580%.</li> <li>Flip each gate to blocking once it passes.</li> </ol> <p>End state \u2014 all blocking and green: <code>pytest</code>, coverage \u226580%, <code>ruff</code>, <code>mypy --strict</code>, <code>lint-imports</code>. <code>pip-audit</code> runs advisory (non-blocking) to avoid surprise breakage from new advisories. Actions are SHA-pinned with least-privilege <code>permissions</code>.</p>"},{"location":"adr/0005-ci-quality-gates/#consequences","title":"Consequences","text":"<ul> <li>CI was useful immediately and never blocked progress on pre-existing debt.</li> <li>Quality cannot regress below the current bar.</li> <li>Adding new rule sets (e.g. ruff <code>D</code>/<code>PL</code>/<code>C901</code>) is deferred \u2014 they would add   ~789 errors (mostly docstrings) and belong to a dedicated pass.</li> </ul>"},{"location":"adr/0006-distribution-name/","title":"6. Distribution name vs. the import package","text":"<ul> <li>Status: Accepted (applied in 0.9.0)</li> <li>Date: 2026-06-23</li> </ul>"},{"location":"adr/0006-distribution-name/#context","title":"Context","text":"<p>The project is named Warp, which collides with several well-known projects:</p> <ul> <li>NVIDIA Warp (<code>warp-lang</code> on PyPI) \u2014 a Python framework for GPU simulation.</li> <li>Warp (warpdotdev) \u2014 a popular terminal.</li> <li>Other smaller uses of \"warp\" across ecosystems.</li> </ul> <p>The PyPI distribution name <code>warp</code> is effectively unavailable / ambiguous, and the name is hard to discover and search for.</p>"},{"location":"adr/0006-distribution-name/#decision","title":"Decision","text":"<p>If/when publishing to PyPI, use a namespaced distribution name while keeping the import package as <code>warp</code>:</p> <ul> <li>distribution name: e.g. <code>warp-engine</code> (or <code>warp-crud</code>, <code>warp-api</code>)</li> <li>import name: <code>warp</code> (unchanged) \u2014 <code>pip install warp-engine</code> then <code>import warp</code></li> </ul> <p>This is a metadata-only change (<code>[project] name</code> in <code>pyproject.toml</code>) and does not affect source code. Re-evaluate the import name only if it proves confusing.</p>"},{"location":"adr/0006-distribution-name/#consequences","title":"Consequences","text":"<ul> <li>Avoids a name clash on PyPI and improves discoverability.</li> <li>Project marketing/name (\"Warp Engine\") stays intact.</li> <li>Deferred until publication; tracked here so it is not forgotten.</li> </ul>"},{"location":"adr/0007-hexagonal-architecture/","title":"7. Hexagonal architecture (ports &amp; adapters)","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-09-17</li> <li>Supersedes: ADR-0001</li> </ul>"},{"location":"adr/0007-hexagonal-architecture/#context","title":"Context","text":"<p>The layered contracts of ADR-0001 kept packages from importing \"upwards\", but they could not express the properties that actually matter for this code base: the domain must not know FastAPI or asyncpg; the analysis use case must not construct database readers, an LLM client and a file store itself; and the wiring must exist in exactly one place. In practice composition was duplicated across <code>main.py</code>, <code>cli.py</code> and <code>integration/pipeline.py</code> (with a bug in two of them), the catalog file store mixed persistence with the review workflow, an HTTP handler built the whole enrichment pipeline inline, and module-level singletons (<code>AppState</code>, <code>_settings</code>, <code>_auth_manager</code>) made the app hard to test.</p>"},{"location":"adr/0007-hexagonal-architecture/#decision","title":"Decision","text":"<p>Four layers with a strict inward dependency direction, enforced by import-linter (<code>[tool.importlinter]</code> in <code>pyproject.toml</code>):</p> Layer Package Rules Entry points <code>warp.main</code>, <code>warp.cli</code> may import anything; own the process (uvicorn, console script) Composition root <code>warp.infrastructure</code> builds outbound adapters and the <code>Container</code>; loads config; sets up logging Adapters <code>warp.adapters.inbound.{http,cli}</code>, <code>warp.adapters.outbound.{db,llm,catalog_store,export}</code> may import <code>application</code> and <code>domain</code> only; inbound and outbound are independent of each other, as are http and cli Application <code>warp.application</code> use-case services, ports (<code>typing.Protocol</code>), configuration models, the <code>Container</code> type; imports <code>domain</code> only; no frameworks/drivers Domain <code>warp.domain</code> entities and pure rules (stdlib + pydantic); imports nothing else <p>Key mechanics:</p> <ul> <li>Ports (<code>warp.application.ports</code>): <code>DatabaseGateway</code>/<code>SqlReader</code>/   <code>DatabaseGatewayFactory</code>, <code>CommentSource</code>/<code>SampleSource</code>,   <code>CatalogRepository</code>, <code>TextGenerator</code>, <code>CatalogExporter</code>. Adapters satisfy   them structurally; tests substitute fakes without patching.</li> <li>Composition root: <code>infrastructure.bootstrap.build_container(settings,   *, repository=, gateway_factory=, text_generator_factory=, analysis_factory=)</code>   returns an <code>application.container.Container</code>. The HTTP app takes a   container factory (<code>create_app(container_factory, env)</code>), the CLI group   receives one through <code>context_settings[\"obj\"]</code>; neither adapter imports   <code>infrastructure</code>.</li> <li>Runtime state lives on <code>app.state.runtime</code> (<code>RuntimeContext</code>) and in   <code>RuntimeEnv</code>, not in module globals.</li> <li>Review workflow transitions are pure functions in   <code>domain.catalog_review</code>; <code>CatalogReviewService</code> applies them over the   repository; <code>CatalogFileStore</code> only persists.</li> <li>No compatibility shims: 0.9.0 changes import paths outright.</li> </ul>"},{"location":"adr/0007-hexagonal-architecture/#consequences","title":"Consequences","text":"<ul> <li>Import paths changed (breaking); the test tree mirrors the layers.</li> <li>Any adapter can be swapped in tests or by embedders (in-memory repository,   fake gateway, recorded LLM) by passing it to <code>build_container</code>.</li> <li>The full startup sequence is exercised in tests over a fake gateway.</li> <li>Adding a database or LLM provider means adding an outbound adapter that   satisfies the port; no application code changes.</li> </ul>"},{"location":"adr/0008-dialect-abstraction-and-odbc-adapter/","title":"8. Dialect abstraction and the ODBC (SQL Server) adapter","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-09-17</li> </ul>"},{"location":"adr/0008-dialect-abstraction-and-odbc-adapter/#context","title":"Context","text":"<p>Warp supported PostgreSQL (asyncpg) and MySQL (aiomysql). Everything that differs between the two \u2014 placeholder style (<code>$1</code> vs <code>%s</code>), identifier quoting, <code>ILIKE</code> vs <code>LIKE</code>, <code>RETURNING</code>, <code>LIMIT/OFFSET</code>, the default schema and the catalog queries for comments and row counts \u2014 was expressed as <code>if dialect == \"postgresql\"</code> branches spread over six modules. Adding a third engine meant touching all of them.</p> <p>SQL Server was the requested third engine. There is no maintained native asyncio driver for it; the standard route is ODBC (<code>pyodbc</code>, wrapped for asyncio by <code>aioodbc</code>) with Microsoft's <code>msodbcsql18</code> driver. ODBC also opens the door to other data sources (Oracle, DB2, \u2026) through the same code path.</p>"},{"location":"adr/0008-dialect-abstraction-and-odbc-adapter/#decision","title":"Decision","text":"<ol> <li>One <code>Dialect</code> object per engine (<code>adapters/outbound/db/dialect.py</code>), a    frozen dataclass holding the placeholder style, quote characters, <code>LIKE</code>    operator, how written rows are returned (<code>returning</code> / <code>output</code> / <code>refetch</code>),    pagination style (<code>LIMIT/OFFSET</code> or <code>OFFSET \u2026 FETCH</code>), percent escaping,    default schema and the comment / row-count catalog queries. The query    builder, the named-parameter binder, identifier quoting, the metadata    readers and the composition root consume the <code>Dialect</code>; none of them branch    on a database-type string any more. PostgreSQL and MySQL SQL is unchanged    byte for byte.</li> <li>A single <code>ODBCAdapter</code> with two profiles selected by the config <code>type</code>:</li> <li><code>mssql</code> / <code>sqlserver</code> \u2014 first-class: introspection through      <code>INFORMATION_SCHEMA</code> and <code>sys.*</code> (identity/computed markers, keys,      indexes), writes that return the row with <code>OUTPUT</code> (falling back per table      to <code>SCOPE_IDENTITY()</code> + re-select when triggers make <code>OUTPUT</code> illegal,      error 334), <code>TOP</code>/<code>OFFSET \u2026 FETCH</code>, a <code>datetimeoffset</code> output converter,      comments from <code>MS_Description</code> extended properties and row counts from      <code>sys.partitions</code>. Covered by real-database integration tests in CI.</li> <li><code>odbc</code> \u2014 best effort: ANSI quoting, <code>?</code> placeholders, introspection via      the ODBC catalog functions, re-select after writes, no catalog      intelligence. Unit-tested only; documented as such.</li> <li>Packaging: <code>aioodbc</code> lives in an <code>odbc</code> extra and is imported lazily in    <code>connect()</code>, so the factory and the unit tests run without it. The ODBC    driver is a system package: the Docker image installs <code>msodbcsql18</code> by    default (<code>--build-arg WITH_MSSQL_ODBC=0</code> opts out), CI installs it on the    Ubuntu runner, developers install it locally (<code>brew install msodbcsql18</code> /    apt). Both hashed lock files include <code>aioodbc</code>/<code>pyodbc</code>.</li> <li>Tests: SQL Server integration tests run in CI (<code>WARP_REQUIRE_MSSQL=1</code>    turns \"unavailable\" into a failure) and auto-skip on machines without the    driver or without an x86-64 Docker engine (the SQL Server image has no    arm64 build). <code>WARP_MSSQL_HOST</code> points them at an existing server instead.</li> </ol>"},{"location":"adr/0008-dialect-abstraction-and-odbc-adapter/#consequences","title":"Consequences","text":"<ul> <li>Adding an engine is now one <code>Dialect</code> entry plus, when the driver's   execution model differs, one adapter; the SQL helpers need no changes.</li> <li><code>ColumnSchema.is_auto_generated</code> gives create models and required-column   checks one definition of \"the database fills this in\" (auto-increment,   serial, identity, computed). PostgreSQL identity columns are now recognised   too.</li> <li>SQL Server identifiers are bracket-quoted through the same strict   sanitizer, so the identifier-injection guarantees are unchanged.</li> <li>The generic profile cannot detect identity columns unless the driver reports   them in <code>TYPE_NAME</code>, and <code>DELETE</code> costs an extra <code>SELECT</code> because rowcounts   may be unreported; both are acceptable for a best-effort path.</li> <li><code>aioodbc</code> 0.5's <code>statistics()</code> wrapper drops pyodbc's mandatory table   argument; the adapter works around it and degrades to \"no index metadata\"   rather than failing.</li> </ul>"}]}